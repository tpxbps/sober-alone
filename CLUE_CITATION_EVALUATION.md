# 引用协议模型验收 · 2026-09-19

本次先合并当前公共模型注册表与配套调用路径，再使用公开虚构材料评估全部 9 个在用模型。所有调用经过既有网关，使用真实 speech 模型工厂、temperature=0.8、角色提示词和每次调用的阶段规则。没有发送私有试点剧本，没有自动重试或付费修复循环。

共 43 次请求，服务返回的总 token 用量为 51,474（输入与输出合计，包含缓存命中；这不是费用估算）。19 次基线、15 次规则补强后评估、9 次针对既往问题模型的固定规则复测。高阶模型仅各 1 次，避免为简单语法投入过多费用。

| 模型 | 基线通过 | 补强规则后通过 |
| --- | --- | --- |
| DeepSeek v4.1 Flash（deepseek-flash） | 2/3 | 5/6 |
| Qwen3.8 Flash | 2/3 | 5/6 |
| GLM-5.3 Flash | 1/3 | 6/6 |
| Doubao Seed 2.0 Lite | 2/3 | 3/3 |
| HY3 | 3/3 | 3/3 |
| Kimi K3 | 1/1 | 未追加 |
| Qwen3.8 Max 0902 | 1/1 | 未追加 |
| GLM-5.3 | 1/1 | 未追加 |
| DeepSeek v4 Pro 0813 | 1/1 | 未追加 |

普通模型从基线 10/15 提高到补强后两批合计 22/24。小样本不能当作总体可靠率；高阶模型通过只说明一次明确双模式任务成功，未覆盖自然追问及长局面。相同最终提示词下保留了两次失败：Qwen 一次遗漏推理首括号，DeepSeek 一次自然追问仍仅使用合法旧式引用。均没有未知证据在宿主显示为有效引用。

规则补强要求基于公开线索作判断时至少给一个核心判断附证据，明确即使历史仅有旧格式也要遵守，并避免在纠正旧错误时再输出未公开 ID。宿主将裸多 ID 组降级成独立标签，继续执行已公开 ID 白名单、消息权限快照及流式过滤；不会猜测应当标注哪段原文。容错成功仍不能让原始格式失败变成模型通过。

验收包含三类场景：明确要求直接标签与多证据关联、无格式提示的自然追问、带旧标签和未知 ID 的错误历史。检查同时覆盖非空输出、所需引用形式、引用证据范围、未知 ID、畸形 ID 组及流式一致性。第一类必须出现至少两条证据组成的关联引用；第三类只要求覆盖两条材料，允许分别引用。初版第三类评分误要求两条证据在同一个关联组，补强批次中的三条合法输出因此被误判。已修正评分规则、补充评分器测试；原始请求和结果保持不变，重算结果另存，未增加或改写模型输出。

完整原始报告、逐条检查及离线重算记录随私有验收包归档。公共可重复运行入口为 `backend/scripts/clue_citation_eval.py`，样例为 `fixtures/clue-citation-eval.json`。普通 CI 不运行真实模型。此轮结果支持多数尝试能遵循的功能目标；未宣称所有模型每次成功，也未将语法通过等同于完整游戏推理质量验收。
